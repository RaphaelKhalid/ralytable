# Loop 40: equivalence-aware train/eval splitting

This bounded CPU probe creates semantic program families with alpha-renamed
and reordered surface variants. It compares example-level random splitting
with group-level splitting and counts semantic families crossing train/eval.

Run:

```text
python experiments/59_equivalence_aware_split/split_probe.py
```
