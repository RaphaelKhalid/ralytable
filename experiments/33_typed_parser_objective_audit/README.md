# Experiment 33 — typed parser objective audit

Dependency-free smoke audit for the preregistered parser-data contract. It
checks that clean records contain only prompt, typed graph, public execution
examples, and declared counterfactuals, while deliberately contaminated
records are rejected.

Run `python objective_audit.py`.
