# Experiment 35 — binding and alpha-renaming invariance

Dependency-free probe for systematic code generalization. It compares a
surface/position-sensitive representation with an alpha-normalized typed graph
under variable renaming, declaration reordering, binding edits, and unused
distractors.

Run `python binding_probe.py`.
