# Experiment 27 — ledger field steganography audit

Dependency-free adversarial control for typed-ledger designs. It compares a
semantic renderer with renderers that hide the operation in an unused metadata
or provenance field. State-level causal tests can miss this loophole; this
probe varies each field independently while holding declared semantics fixed.

Run `python field_audit.py`.
