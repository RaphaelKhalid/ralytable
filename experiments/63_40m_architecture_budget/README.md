# Loop 44: explicit 40M architecture budget

This bounded calculation turns the accumulated contracts into a concrete
parameter envelope. It counts a byte-level semantic parser, typed graph
router, module/retrieval interfaces, and verifier heads while reserving zero
parameters for an opaque residual bypass.

Run:

```text
python experiments/63_40m_architecture_budget/architecture_budget.py
```
