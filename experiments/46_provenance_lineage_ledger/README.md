# Loop 27: provenance lineage ledger

This bounded CPU probe tests explicit source lineage through a multi-step
dataflow graph. It compares value-only state, one-hop parent metadata, and a
recursive lineage ledger.

Run:

```text
python experiments/46_provenance_lineage_ledger/lineage_probe.py
```
