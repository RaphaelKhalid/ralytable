# Loop 33: deterministic candidate ranking

This bounded CPU probe tests exact score comparison, proof filtering, stable
tie-breaking, and near-tie behavior for speculative candidates.

Run:

```text
python experiments/52_deterministic_candidate_ranking/ranking_probe.py
```
