# Loop 41: typed primitive retrieval on novel compositions

This bounded CPU probe separates whole-program memorization from composition
of retrieved typed primitives. Training stores some pipelines; evaluation
uses held-out combinations built from the same primitives.

Run:

```text
python experiments/60_typed_library_novel_composition/library_probe.py
```
