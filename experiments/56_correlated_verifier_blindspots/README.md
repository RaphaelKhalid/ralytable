# Loop 37: correlated verifier blind spots

This bounded CPU probe tests candidates that fool every standard verifier.
It compares unanimous type/effect/execution agreement with a gate that also
requires an independent metamorphic challenge.

Run:

```text
python experiments/56_correlated_verifier_blindspots/blindspot_probe.py
```
