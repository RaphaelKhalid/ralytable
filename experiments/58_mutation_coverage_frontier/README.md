# Loop 39: mutation coverage frontier

This bounded CPU probe compares surface-only, typed-core, and structural
metamorphic suites against constant, first-argument, commutative-only, and
surface-sensitive shortcuts. It records which shortcut families each suite
kills.

Run:

```text
python experiments/58_mutation_coverage_frontier/mutation_probe.py
```
