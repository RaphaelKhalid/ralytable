# Loop 25: proof compression frontier

This bounded CPU calculation compares flat path certificates, reusable proof
DAGs, and hash-only references under fixed output budgets. It measures the
cost advantage of sharing common derivations while retaining enough local
structure for deterministic auditing.

Run:

```text
python experiments/44_proof_compression_frontier/compression_probe.py
```
