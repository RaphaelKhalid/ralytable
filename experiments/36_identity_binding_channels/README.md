# Experiment 36 — explicit identity channels

Dependency-free probe of the identity-retention fix for structured coders.
It compares an opaque raw residual, an explicit typed copy table, and compact
hashed slots. The task is to retrieve a named entity after distractors and a
query reorder; identity recall is measured separately from causal/no-bypass
properties.

Run `python binding_channels.py`.
