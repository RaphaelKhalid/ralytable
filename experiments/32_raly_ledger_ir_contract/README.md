# Experiment 32 — Raly-aligned ledger IR contract

Dependency-free contract probe for mapping the proposed ledger to Raly's
existing typed concepts: dimension, VSA family, load/capacity, role schema,
provenance, and explicit dataflow. It tests canonical round-trips and
rejection of malformed or over-capacity graphs.

Run `python ir_contract.py`.
