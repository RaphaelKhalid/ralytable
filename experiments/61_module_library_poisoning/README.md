# Loop 42: module-library poisoning and provenance

This bounded CPU probe compares name-only retrieval, type-only retrieval, and
content-addressed trusted-module retrieval when a poisoned implementation
shares a trusted module's public signature.

Run:

```text
python experiments/61_module_library_poisoning/module_probe.py
```
