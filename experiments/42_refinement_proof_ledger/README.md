# Loop 23: refinement proof ledger

This bounded CPU probe tests semantic preconditions such as nonzero divisors
and bounded indices. It compares base-type checking, unverified predicate
annotations, and a proof ledger that requires a certificate for each
refinement obligation.

Run:

```text
python experiments/42_refinement_proof_ledger/refinement_probe.py
```
