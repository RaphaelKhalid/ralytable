# Loop 46: Raly integration slice audit

This read-only audit maps the surviving research contracts onto the existing
Raly compiler boundaries and identifies the smallest safe prototype. It does
not change compiler behavior or train a model.

Run:

```text
python experiments/65_raly_integration_slice/integration_slice.py
```
