# Loop 43: transitive capability closure

This bounded CPU probe tests whether wrapper modules can hide effects from a
caller capability budget. It compares declared-effect checking, shallow
direct-effect checking, and recursive transitive effect closure.

Run:

```text
python experiments/62_transitive_capability_ledger/capability_probe.py
```
